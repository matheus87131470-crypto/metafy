# Metafy Backend - Environment Variables

# OpenAI API Key (para análises de IA)
OPENAI_API_KEY=sk-your-openai-api-key-here

# ── Asaas PIX ─────────────────────────────────────────────
# Acesse https://app.asaas.com > Configurações > Integrações > API
# Para sandbox: https://sandbox.asaas.com > Configurações > API
ASAAS_API_KEY=$aact_your_api_key_here

# 'sandbox' (testes) ou 'production' (produção)
ASAAS_ENV=sandbox

# URL pública do backend (usado para registrar webhook no Asaas)
# Formato: https://seu-backend.onrender.com
# Webhook a configurar: POST {SITE_URL}/api/webhooks/pix
SITE_URL=https://metafy.store

# Port (Render define automaticamente)
PORT=3000

# ── API-Football (api-sports.io) ──────────────────────────
# 1. Acesse https://dashboard.api-football.com e crie uma conta gratuita
# 2. Copie a API Key em "My Account > API Key"
# 3. Plano Free: 100 req/dia — cache de 30 min é suficiente para produção
# 4. Usado em: GET /api/top-picks/today e GET /api/games/today
API_FOOTBALL_KEY=your_api_football_key_here
